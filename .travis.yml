language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "xiaoshaliwu"
  - git config user.email "xiaoshaliwu@xiaoshaliwu.tk"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${gh_token}@${GH_REF}" master:master
  - git push --force --quiet "https://${code_t}@${GH_REE}" master:master
# E: Build LifeCycle

branches:
  only:
    - hexo
env:
  global:
   - GH_REF: github.com/xiaoshaliwu/xiaoshaliwu.github.io.git
   - GH_REE: git.coding.net/xiaoshaliwu/xiaoshaliwu.coding.me.git
